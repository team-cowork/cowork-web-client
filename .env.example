# DataGSM PKCE OAuth 연동 설정 (모두 서버 전용 — 클라이언트 번들에 노출되지 않음)

# DataGSM에 등록된 클라이언트 ID (authorize 요청에 사용)
DATAGSM_CLIENT_ID=

# Cowork Authorization API(code/verifier 교환·토큰 갱신·로그아웃) 베이스 URL
# 예: https://auth.cowork.example.com/api
AUTH_API_BASE_URL=
